# Variables de Entorno para Railway - CONFIGURAR EN EL PANEL DE RAILWAY

# =====================================================
# BASE DE DATOS POSTGRESQL - Variables de Railway
# =====================================================
# Railway genera automáticamente estas variables cuando creas el servicio PostgreSQL.
# NO necesitas configurarlas manualmente, Railway las proporciona automáticamente:

# DATABASE_URL - Railway la genera automáticamente usando variables internas
# DATABASE_PUBLIC_URL - Para conexiones externas (si es necesario)
# PGDATABASE=railway (generado automáticamente)
# PGUSER=postgres (generado automáticamente)  
# POSTGRES_PASSWORD=qOtlUPkWmalWRclfzYbujoIYTffbjYLC (generado automáticamente)
# PGHOST - Railway lo genera automáticamente
# PGPORT=5432 - Railway lo configura automáticamente

# =====================================================
# VARIABLES QUE SÍ DEBES CONFIGURAR EN RAILWAY PANEL
# =====================================================

# Configuración del entorno
NODE_ENV=production

# JWT Secrets (CAMBIAR POR VALORES SEGUROS)
JWT_SECRET=chpc-jwt-secret-super-seguro-railway-2026
JWT_REFRESH_SECRET=chpc-refresh-secret-super-seguro-railway-2026

# Frontend URLs para Railway
FRONTEND_URL=https://frontend-liart-two-99.vercel.app
# CORS - URLs permitidas (separadas por coma)
CORS_ORIGIN=https://frontend-liart-two-99.vercel.app,https://frontend-liart-two-99.vercel.app/

# Puerto (Railway lo asigna automáticamente, no configurar)
# PORT=${{PORT}}

# Email Configuration (si usas nodemailer)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=tu-email@gmail.com
MAIL_PASS=tu-app-password

# Rate Limiting para producción
THROTTLE_TTL=60
THROTTLE_LIMIT=100