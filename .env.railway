# Variables de Entorno para Railway - CONFIGURAR EN EL PANEL DE RAILWAY

# Base de Datos (Railway PostgreSQL automática)
# Railway genera automáticamente DATABASE_URL, pero puedes usar una externa
# DATABASE_URL=postgresql://usuario:password@host:5432/database_name

# JWT Secrets (CAMBIAR POR VALORES SEGUROS)
JWT_SECRET=tu-jwt-secret-super-seguro-para-railway
JWT_REFRESH_SECRET=tu-jwt-refresh-secret-super-seguro-para-railway

# Configuración del Entorno
NODE_ENV=production

# Frontend URLs para Railway
FRONTEND_URL=https://tu-frontend-app.vercel.app
# CORS - URLs permitidas (separadas por coma)
CORS_ORIGIN=https://tu-frontend-app.vercel.app,https://www.tu-dominio.com

# Puerto (Railway lo asigna automáticamente)
PORT=${{PORT}}

# Email Configuration (si usas nodemailer)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=tu-email@gmail.com
MAIL_PASS=tu-app-password

# Rate Limiting para producción
THROTTLE_TTL=60
THROTTLE_LIMIT=100